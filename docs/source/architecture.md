# Architecture

The canonical architecture document lives at the repository root
([`ARCHITECTURE.md`](https://github.com/thanhndv212/figaroh-plus/blob/main/ARCHITECTURE.md))
so it stays next to the source tree it describes. This page embeds it
directly so the same content is browsable as part of the docs site,
without a second copy to keep in sync.

{% include-markdown "../../ARCHITECTURE.md" heading-offset=1 %}
